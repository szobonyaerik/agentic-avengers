{
  "_comment": [
    "`amendments` holds requirement-id references ONLY — [\"A1\", \"A2\"] — and nothing else. The",
    "records themselves live in the phase's amendments.json; a verdict then reads 'verified at",
    "attempt N, plus amendments A1..An'. It is the one schema extension since the schema was frozen,",
    "made at the schema (skills/verifier-triage) rather than as a bespoke top-level key.",
    "",
    "`execution` replaced `test_quality`, at the schema, for the same reason: `test_quality.reviewed`",
    "was a boolean the verifying agent wrote about itself and every gate believed. `chain` is the",
    "head of the hash chain over verification-evidence.json's recorded runs; the gates refuse a pass",
    "whose chain does not match the transcript on disk. `run` no longer carries a family comparison:",
    "the cross-family reading pass it described is gone, and a field asserting an independence",
    "nothing performs reads as coverage that is not there.",
    "",
    "`method` on every finding is the third extension, at the schema (issue #96): how the finding was",
    "established and how its fix is confirmed - what was DRIVEN, over which axes. A sweep done by",
    "reading reported complete and missed two instances; a probe that drove one axis of two scoped a",
    "fix round that closed half a defect. scripts/verifier_precheck.py refuses a finding without it.",
    "",
    "`unverified_by_gate` is the fourth extension, at the schema (issue #116): every `human-observed`",
    "condition of the phase's Done when, carried verbatim from plan.md's table, each stating that",
    "THIS verdict did not verify it. faithful-rep phase 3 passed on 162 tests and 23 recorded runs",
    "with nothing rendered, over a Done when that is what the captain SEES; the pass was honest",
    "about its tests and silent about its blind spot, and two visual defects shipped through that",
    "silence. scripts/verifier_precheck.py refuses a `pass` that omits one, and the same list goes",
    "on the contract card. A gate-verifiable condition belongs to the ordinary verdict and never here.",
    "",
    "A finding's `status` has a fourth value, `deferred`, with `deferred_to` naming the owning phase",
    "(issue #115). It resolves the finding ONLY when the phase's carried.json records a deferral",
    "naming it - `scripts/carried_items.py defer`, gated on the phase's structured Done when and",
    "carrying the measurement. The stamp alone is an open finding; `bypassed` stays false, since a",
    "deferral is not a waiver."
  ],
  "feature": "<feature>",
  "phase": "<n>-<slug>",
  "readers": ["phase-handover @ per phase", "feature close @ once"],
  "attempt": 1,
  "superseded_attempts": [
    {
      "attempt": 1,
      "verdict": "fail",
      "outcome": "<one line — the attempt itself is archived, never nested here>",
      "archived_to": "verdict-attempt-1.json"
    }
  ],
  "amendments": ["A1"],
  "report": "<= 1500 chars: the headline judgement, plus only what the structured fields cannot say>",
  "run": {
    "at": "YYYY-MM-DDThh:mm:ssZ"
  },
  "tests": {
    "total": 0,
    "passed": 0,
    "failed": 0
  },
  "coverage": {
    "requirements": 0,
    "traced": 0,
    "untraced": []
  },
  "execution": {
    "evidence": "verification-evidence.json",
    "chain": "<scripts/verifier_evidence.py chain <phase-dir> \u2014 the transcript this verdict stands on>",
    "runs": 0
  },
  "findings": [
    {
      "id": "<12-char hash of kind|spec_id|normalized_target>",
      "kind": "code",
      "spec_id": "R<n>.<k>.<m>",
      "target": "<repo-relative test/file/requirement>",
      "severity": "blocker",
      "instruction": "<concrete fix directions for the routed agent>",
      "method": "<how this was established and how its fix is confirmed: what was DRIVEN, over which axes - never 'read'>",
      "route_to": "avenger-backend-architect",
      "status": "open",
      "deferred_to": null,
      "break_glass": false,
      "waiver_reason": null,
      "waived_by": null,
      "waived_at": null
    }
  ],
  "unverified_by_gate": [
    {
      "id": "DW-<n>",
      "outcome": "<the plan's Done when row, verbatim - a paraphrase is where a limitation softens>",
      "verified_by_this_verdict": false
    }
  ],
  "mutation": {
    "enabled": false
  },
  "routed": [
    {
      "to": "avenger-backend-architect",
      "finding_id": "<hash>",
      "reason": "code",
      "spec_id": "R<n>.<k>.<m>"
    }
  ],
  "verdict": "fail",
  "bypassed": false
}
