Use the agent-factory agent to package this pipeline as a reusable template: bundle
.claude/agents/, .claude/skills/, scripts/, prompts/, the settings.json hooks block, and the
CLAUDE.md conventions into a plugin/template I can drop into a new repo, parameterizing the
OpenRouter model ids and the feature-id paths.